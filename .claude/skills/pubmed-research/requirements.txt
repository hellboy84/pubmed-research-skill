requests>=2.28

# pypdf powers the Unpaywall stage of `fulltext`, which extracts text from
# open-access PDFs. Without it that stage still returns the PDF/landing URLs,
# but no text.
pypdf>=4.0

# lxml is optional; if installed it is used automatically for faster, more
# robust XML parsing. The skill falls back to the stdlib xml.etree otherwise.
# lxml>=4.9
